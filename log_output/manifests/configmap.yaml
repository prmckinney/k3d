apiVersion: v1
kind: ConfigMap
metadata:
  name: log-configmap
  namespace: exercises
data:
  message: "hello world"
  information.txt: |
    this text is from a file
  postgres_host: "postgres-svc"
  postgres_port: "5432"
  postgres_db: "postgres"
  postgres_user: "postgres"
  postgres_password: "password"
  init.sql: |
    CREATE TABLE pingpong(ping INT);
    INSERT INTO pingpong VALUES (0);
