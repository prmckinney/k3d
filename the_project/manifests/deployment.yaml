apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-app-dep
  namespace: project
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: shared-todo
          persistentVolumeClaim:
            claimName: todo-claim
      containers:
        - name: todo-app
          image: prmckinney/k3d_todo_app:2.6
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "8080"
            - name: BACKEND_URL
              value: "http://localhost:3000"
        - name: todo-backend
          image: prmckinney/k3d_todo_backend:2.8
          imagePullPolicy: Always
          env:
            - name: PORT
              value: "3000"
            - name: DIR
              value: "/usr/src/app/files"
            - name: POSTGRES_PASSHOST
              valueFrom:
                configMapKeyRef:
                  name: todo-configmap
                  key: postgres_passhost
            - name: POSTGRES_PASSPORT
              valueFrom:
                configMapKeyRef:
                  name: todo-configmap
                  key: postgres_passport
            - name: POSTGRES_PASSDB
              valueFrom:
                configMapKeyRef:
                  name: todo-configmap
                  key: postgres_passdb
            - name: POSTGRES_PASSUSER
              valueFrom:
                configMapKeyRef:
                  name: todo-configmap
                  key: postgres_passuser
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: todo-configmap
                  key: postgres_password
          volumeMounts:
            - name: shared-todo
              mountPath: /usr/src/app/files
